Brass Tacks owner UI polish v35

Replace these paths in your repository:
  site/app.html
  backend/tests/test_site_build.py
  web/app/index.html  (already rebuilt; optional if your deployment rebuilds from site/)

Then verify:
  pytest -q backend/tests/test_site_build.py
  python scripts/build_web.py

What changed:
  - Desktop recommendation content scrolls instead of being clipped by the footer.
  - The unused artwork column is removed; the action plan uses the right side.
  - Feed type scale, spacing, footer, and controls are tightened.
  - Growth process and empty states use dark midnight surfaces instead of white.
  - The progress preview fills its card instead of leaving a large blank area.
  - Mobile recommendation text uses the full width instead of a hidden icon column.
